version: '3'
services:
  web:
    image: python:3.9-slim
    working_dir: /app
    volumes:
      - .:/app
    command: bash -c "pip install -r requirements.txt && python main.py"
    ports:
      - "5000:5000"
    restart: always